{
  "name": "[Midnight pre-prod] Peer count low",
  "type": "query alert",
  "query": "min(last_5m):avg:gcp.prometheus.substrate_sub_libp2p_peers_count{env:preprod} by {host} < 3",
  "message": "{{#is_warning}}@slack-node-ops-alerts\nPeer count low on {{host.name}} (warning). Peers trend down BEFORE height stalls — act now. Check outbound networking/firewall, bootnode/topology config, and version/chainspec match.{{/is_warning}}\n{{#is_alert}}@webhook-squadcast Peer count critically low / zero on {{host.name}} — node will fall out of sync imminently.{{/is_alert}}\n{{#is_recovery}}Peer count recovered on {{host.name}}.{{/is_recovery}}",
  "tags": ["env:preprod", "service:midnight-node", "team:node-ops", "source:gmp"],
  "priority": 2,
  "options": {
    "thresholds": { "warning": 3, "critical": 1 },
    "notify_no_data": true,
    "no_data_timeframe": 15,
    "renotify_interval": 60,
    "include_tags": true
  }
}
